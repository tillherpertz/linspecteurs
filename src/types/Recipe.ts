export default interface Recipe {
    collectionId: string;
    collectionName: string;
    created: string;
    headline: string;
    id: string;
    image: string;
    ingredients: JSON;
    instructions: JSON;
    subHeadline: string;
    updated: string;
}